[project]
name = "agent-lifecycle-reference-test"
version = "0"
requires-python = ">=3.12"
dependencies = [
    "langgraph==1.2.10",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
